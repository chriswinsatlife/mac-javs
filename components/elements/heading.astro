---
const { color = 'dark/light', className, ...props } = Astro.props;
---

<h1
  class:list={[
    'font-display text-5xl/12 tracking-tight text-balance sm:text-[5rem]/20',
    color === 'dark/light' && 'text-mist-950 dark:text-white',
    color === 'light' && 'text-white',
    className,
  ]}
  {...props}
>
  <slot />
</h1>