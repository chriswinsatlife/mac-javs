---
export interface Props {
  className?: string
}

const { className, ...props } = Astro.props
const computedClass = ((className || '') + ' grid grid-cols-1 text-base/6 sm:grid-cols-[min(50%,--spacing(80))_auto] sm:text-sm/6').trim()
---

<dl
  {...props}
  class={computedClass}
/>